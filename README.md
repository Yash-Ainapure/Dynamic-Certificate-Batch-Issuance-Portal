# Dynamic Certificate Batch Issuance Portal

Monorepo with `server` (Node/Express/TS/Prisma) and `client` (React/TS/Vite/Tailwind). This repository is currently scaffolded with placeholder files per project plan.

- Server: `server/`
- Client: `client/`

Setup instructions will be added after implementation.
